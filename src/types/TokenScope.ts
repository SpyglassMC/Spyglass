export const TokenScope = {
    'comment.line.number-sign.mcfunction': 'comment.line.number-sign.mcfunction',
    'string.quoted.double.mcfunction': 'string.quoted.double.mcfunction',
}

export type TokenScope = keyof typeof TokenScope
