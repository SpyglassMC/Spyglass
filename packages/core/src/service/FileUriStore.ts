/**
 * A data structure for storing a list of file URIs efficiently.
 *
 * Currently implemented naively with trees.
 */
export class FileUriStore {
	#tree = {}

	add(uri: string) {}

	addAll(store: FileUriStore) {
	}

	has(uri: string) {
	}

	delete(uri: string) {
	}

	getChildrenOf(uri: string) {
	}

	getDescendentsOf(uri: string) {
	}
}
